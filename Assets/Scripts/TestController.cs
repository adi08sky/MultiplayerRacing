using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        RaceController.racing = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
